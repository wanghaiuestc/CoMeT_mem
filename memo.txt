- Add the mode setting (mode number, latencies, powers, etc) in base.cfg, make sure it can be read using Sim()->getCfg()

- In common/performance_model/dram_perf_model_constant_multimode.cc, m_dram_access_cost[] is not set correctly for now: e.g., [0] is set for normal mode (but should be low power mode). This leads to slow simulation in normal mode. 

- In common/system/print_trace_multimode.cc: wrt[i].write_access_count_per_epoch is always 0? Also check the print results, should the info printed per mode?
